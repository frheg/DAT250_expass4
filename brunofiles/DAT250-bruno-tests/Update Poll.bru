meta {
  name: Update Poll
  type: http
  seq: 9
}

put {
  url: {{base_url}}/polls/{{pollId}}
  body: json
  auth: none
}

headers {
  ~Content-Type: application/json
}

body:json {
  {
    "validUntil": "2025-09-08T20:30:32.389080665Z",
    "publicAccess": false
  }
}

tests {
  test("should update poll", function () {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("poll data should be updated", function () {
    const resBody = res.getBody();
    expect(resBody.validUntil).to.equal('2025-09-08T20:30:32.389080665Z');
    expect(resBody.publicAccess).to.be.false;
  });
}
