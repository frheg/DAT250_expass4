meta {
  name: Get Vote Option by ID
  type: http
  seq: 11
}

get {
  url: {{base_url}}/voteoptions/{{voteOptionId}}
  body: none
  auth: none
}

tests {
  test("should get vote option by ID", function () {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("vote option ID should match", function () {
    const resBody = res.getBody();
    expect(resBody.optionId).to.equal(bru.getVar('voteOptionId'));
  });
}
