meta {
  name: Get User By ID
  type: http
  seq: 4
}

get {
  url: {{base_url}}/users/{{userId}}
  body: none
  auth: none
}

tests {
  test("should get user by ID", function () {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("user ID should match", function () {
    const resBody = res.getBody();
    expect(resBody.userId).to.equal(bru.getVar('userId'));
  });
}
