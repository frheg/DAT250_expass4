meta {
  name: Get All Votes
  type: http
  seq: 15
}

get {
  url: {{base_url}}/votes
  body: none
  auth: none
}

tests {
  test("should get all votes", function () {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("should return an array of votes", function () {
    expect(res.getBody()).to.be.an('array');
  });
}
